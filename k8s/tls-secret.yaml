# TLS Secret for HTTPS (placeholder - generated dynamically in pipeline)
# This file documents the expected secret structure
# Actual secret is created via openssl in the CD pipeline

# The pipeline will create:
# kubectl create secret tls gists-tls-secret \
#   --cert=tls.crt \
#   --key=tls.key \
#   -n istio-system

# For production, use cert-manager with Let's Encrypt:
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: gists-tls
#   namespace: istio-system
# spec:
#   secretName: gists-tls-secret
#   issuerRef:
#     name: letsencrypt-prod
#     kind: ClusterIssuer
#   dnsNames:
#   - gists.kishore.local
